# ==============================================================================
# Propwell AM Platform — Environment Variables
# ==============================================================================
# Copy this file to .env.local and fill in values as needed.
#
#   cp .env.local.example .env.local
#
# The app runs fully in mock mode by default — no API keys or Supabase
# credentials are required for local development or demo.
# ==============================================================================

# --- Supabase ---
# Required for auth + data persistence. Leave as placeholders for mock mode.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# --- Mock Mode ---
# When true (default), the app uses embedded sample data for all API calls.
# Set to "false" to use real Supabase + external APIs.
USE_MOCK=true

# --- External API Keys ---
# Only needed when USE_MOCK=false
RENTCAST_API_KEY=
RENTOMETER_API_KEY=
